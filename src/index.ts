import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

const main = async () => {
  // const result = await prisma.post.create({
  //   data: {
  //     title: "This is Title",
  //     content: "this is content",
  //     authorName: "Miraj hossen",
  //   },
  // });
  // console.log(result);
};
main();
